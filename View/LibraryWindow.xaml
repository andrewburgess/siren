<Window
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	x:Class="View.LibraryWindow"
	x:Name="Window"
	Title="LibraryWindow"
	Width="640" Height="350" WindowStyle="ToolWindow">

	<DockPanel x:Name="LayoutRoot">
		<ListView x:Name="RepositoryList" DockPanel.Dock="Top" Height="200" VerticalAlignment="Top">
			<ListView.View>
				<GridView>
					<GridViewColumn/>
				</GridView>
			</ListView.View>
		</ListView>
		<Button x:Name="AddRepository" Content="Add Location To Library" Width="240" Height="50" HorizontalAlignment="Left" Margin="50,0,0,0"/>
		<Button x:Name="CloseDialog" Content="Done" Width="240" HorizontalAlignment="Right" Height="50" Margin="0,0,50,0" IsDefault="True"/>
	</DockPanel>
</Window>